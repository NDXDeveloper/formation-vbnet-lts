<!-- ============================================================================
     Section 6.2 : XAML fondamentaux et systèmes de layout
     Description : Combine les trois panneaux de la section — DockPanel
                  (coquille : menu en haut, barre d'état en bas, contenu au
                  centre via LastChildFill), Grid (lignes/colonnes Auto/*/fixe,
                  propriétés attachées Grid.Row/Column/ColumnSpan) et
                  StackPanel (pile de boutons). Démontre aussi les ÉVÉNEMENTS
                  ROUTÉS : un seul gestionnaire au niveau du StackPanel capte
                  le Click de tous ses boutons (ButtonBase.Click).
     Fichier source : 02-xaml-layout.md
============================================================================ -->
<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="6.2 — XAML et disposition" Height="360" Width="560"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">

    <DockPanel LastChildFill="True">

        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Fichier" />
            <MenuItem Header="_Édition" />
        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock x:Name="lblStatut" Text="Prêt" />
        </StatusBar>

        <!-- Zone centrale : grille à deux colonnes -->
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Volet : pile de boutons. Le Click est capté AU NIVEAU du panneau -->
            <StackPanel Grid.Column="0" Margin="4" ButtonBase.Click="Boutons_Click">
                <Button Content="Accueil" Margin="0,4" />
                <Button Content="Clients" Margin="0,4" />
                <Button Content="Rapports" Margin="0,4" />
            </StackPanel>

            <!-- Détail : formulaire en grille (Auto / *) -->
            <Grid Grid.Column="1" Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Nom :" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" />
                <TextBox Grid.Row="0" Grid.Column="1" Margin="0,4" />

                <TextBlock Text="Courriel :" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" />
                <TextBox Grid.Row="1" Grid.Column="1" Margin="0,4" />

                <TextBox Grid.Row="2" Grid.ColumnSpan="2"
                         AcceptsReturn="True" TextWrapping="Wrap" Margin="0,8" />
            </Grid>
        </Grid>
    </DockPanel>
</Window>
