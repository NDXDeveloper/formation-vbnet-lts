<!-- =========================================================================
  Section 4.7 : Synchronisation et thread-safety (SyncLock, Interlocked, SemaphoreSlim)
  Description : Projet console VB.NET (.NET 10) — SyncLock, Interlocked,
                SemaphoreSlim (verrou async et throttling) et collections
                concurrentes.
  Fichier source : 07-synchronisation.md
========================================================================== -->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>Synchronisation</RootNamespace>
    <TargetFramework>net10.0</TargetFramework>
    <OptionStrict>On</OptionStrict>
    <OptionExplicit>On</OptionExplicit>
  </PropertyGroup>

</Project>
